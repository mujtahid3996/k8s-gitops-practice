apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nginx-local-tls
  namespace: apps
spec:
  secretName: nginx-local-tls
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
  dnsNames:
    - nginx.local